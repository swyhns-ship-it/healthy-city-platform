# HIA Demo 项目 — 交接简报 (给 Claude Code)

> 本文件作为项目上下文。当你 (Claude Code) 接手此项目时,**完整读一遍本文件**,
> 然后跟用户 (孙文尧, Wenyao) 确认你理解的目标和现状是否对齐, 再开始动手。

---

## 用户

**孙文尧**,同济大学建筑与城市规划学院 (CAUP) 教师, 健康城市方向。
跟王兰教授团队合作,做"健康城市"相关研究。
Python 比较熟,前端不熟,做过大量 GEE/ArcGIS 空间分析。

## 项目背景 (一句话)

**做一个 Streamlit 演示应用, 给上海市卫健委演示"AI 辅助的城市规划项目健康影响评估 (HIA)"。**

这是一个 **Demo**, 不是生产工具。**惊艳 > 完整**。3 个示范案例跑得完美就成功。

## 已有的"前置资产" (在同济 AI 平台上)

孙老师已经在同济大学 AI 平台 (agent.tongji.edu.cn) 上建了一个**HIA 智能体 v0.9**, 包含:
- 7 节点工作流 (Start → 大模型 01 → 代码 00 → 知识库检索 01/02 → 代码 01 → 大模型 02 → End)
- ERF 知识库 v0.3 (1828 条热-健康 ERF)
- 上海城市基线死亡率知识库 v2 (317 条)
- 输入健全性检查、反向场景模板、合理性警告、城市基线定制 (P1-P5)

**Demo 不复用工作流后端**, 而是**用本地 Python 重写计算核心**, 同时保持算法一致。
理由: 网络不稳定 → 演示翻车风险。

## 已做的两个文件 (放在项目根目录)

### `cases.py`
3 个示范案例的完整预设数据, 关键字段:
- 案例 1: 徐汇区漕河泾街道某停车场转 0.5 公顷绿地 (正向, 小规模, 精致)
- 案例 2: 浦东新区花木街道某绿地拆除 1.2 公顷建地铁出口 (反向, 中规模, 升温场景)
- 案例 3: 静安区曹家渡街道某高密度老城新增 5.0 公顷大型公园 (正向, 大规模, 数字震撼)

每个案例包含: 位置 (中心点经纬度 + 多边形)、人口、ΔLST、ΔT_air、基线死亡率、评估期 等。

**位置真实性**: 街道名真实, 但**具体地块和数据为 demo 用途**——卫健委的人一眼能看出是上海的真实街道, 但不用查证。多边形是 Claude 编的, 不精确但合理。

### `hia_engine.py`
HIA 计算的纯函数, 沿用 v0.9 工作流的代码 01 算法:
- 输入: case 字典
- 输出: 主分析 + 敏感性分析的可避免/额外病例数
- 用 Liu 2022 (RR=1.021 per 1°C CVD, 1.014 per 1°C all-cause) 作主分析
- 用 Lingyan 2017 (RR=1.0359 per 1°C, 从 P99 vs MMT 线性外推) 作敏感性
- 自动判断方向 (cooling/warming), 反向场景下报告"额外病例数"

ERF 当前**硬编码**在 cases.py 里 (3 个固定 ERF 字典), Phase 1 不接知识库。

---

## 还要做的事 (按优先级)

### Phase 1 (本周) — Streamlit 骨架
- `app.py` 主应用
- 顶部: 同济 logo + 标题 "上海市城市规划项目健康影响评估专项报告"
- 左侧栏: 示范案例选择 + 显示已填充的项目参数
- 右侧: 上海地图 (Folium), 选中案例后飞到对应位置 + 显示多边形
- 底部: "开始评估" 按钮 → 调用 hia_engine → 显示报告 + 下载 Word

### Phase 2 — Word 报告导出
- 用 `python-docx` 生成正式的 HIA 报告 docx
- 标题页 + 7 章节结构 (跟 v0.9 工作流一致)
- 字体: 中文用宋体 (正文) + 黑体 (标题), 学术正式风格
- 用户点击下载按钮 → 直接生成并下载

### Phase 3 (可选) — 视觉打磨
- 同济红 (#A6192E) 作主色
- 标题字体: Noto Serif SC
- 正文字体: Noto Sans SC
- 关键数字大字突出
- 不要 AI 风格的紫蓝渐变

### Phase 4 (可选, 加分) — "自定义画多边形"按钮
- 允许卫健委用户自己在地图上画多边形
- 但**容易出 Bug**, Phase 1+2 不做。先把示范案例跑透。

---

## 关键技术决策 (已定)

| 决策项 | 选择 | 理由 |
|---|---|---|
| 前端 | Streamlit | 孙老师 Python 强, 不学 JS |
| 地图 | Folium + streamlit-folium | Streamlit 生态最成熟 |
| 计算后端 | 本地 Python (cases.py + hia_engine.py) | 演示稳定, 不依赖网络 |
| ERF | 硬编码 3 个固定 ERF (Liu 2022 + Lingyan 2017) | Demo 简化, 暂停 KB 优化 |
| Word | python-docx | Streamlit + Python 一栈到底 |
| 主色 | 同济红 #A6192E | 用户指定 |
| 字体 | Noto Serif/Sans SC | 学术正式 |
| Logo | 只放同济 | 用户指定 |

## **不要做** 的事

- ❌ 不要重做 ERF 知识库
- ❌ 不要做"通用知识库"
- ❌ 不要写论文相关代码
- ❌ 不要接同济 AI 平台 API (演示用本地)
- ❌ 不要加用户登录、权限管理
- ❌ 不要追求"全上海都能跑"——3 个案例 + 友好挡掉其他位置
- ❌ Phase 1 不做"自定义多边形"
- ❌ 不要用紫色蓝色渐变 (AI 味)

---

## 安装依赖 (新机器第一次)

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install streamlit folium streamlit-folium python-docx
```

## 运行

```bash
streamlit run app.py
```

打开 http://localhost:8501

---

## 报告模板参考

v0.9 工作流生成的报告是 7 章节:

1. 项目概况 (项目名、位置、干预类型、规模、人口、评估期、气候带、场景方向)
2. 暴露变化 (LST + 气温, 表格 + 说明)
3. 健康影响量化结果 (双 ERF 并行, 主分析表 + 年度表)
4. 关键假设与方法说明 (Liu 2022 ERF + Lingyan 2017 ERF + 城市基线死亡率)
5. 敏感性分析的意义
6. 局限性 (8 条左右)
7. 规划建议 (3 条)

报告底部:
**报告生成**: HIA 智能体 (Phase 3.P1+P2+P3+P4)

用 Word 输出时要保留这个结构。

---

## 接下来的具体步骤

1. **读完本文件 + cases.py + hia_engine.py**, 跟用户确认你理解了
2. 开始 Phase 1:
   - 先做最小可跑的 `app.py` (有标题 + 案例选择 + 显示参数, 没地图也行)
   - 跑通后加 Folium 地图
   - 跑通后加"评估"按钮
   - 跑通后加 Markdown 报告渲染
   - 跑通后加 Word 下载
3. 每一步都让用户在浏览器看一眼, 确认 OK 再进下一步

**节奏 > 完整**。

---

## 用户偏好

- 中文回复
- 简明扼要, 不要长篇大论
- 给具体可执行的下一步, 不要给一堆选项让用户选
- 有不确定的地方主动确认, 别瞎猜
- 写代码注释用中文
